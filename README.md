🚀 LLM Quiz Solver TDS Project 2

Gemini-Powered Multi-Step Quiz Automation Agent

An autonomous agent built using LangGraph, LangChain, Playwright, and Gemini 2.5 Flash to automatically solve multi-level quizzes.
It performs web scraping, checksum computation, audio number extraction, data processing, and automated submission — all fully hands-free.

This is the same agent used to solve:
✔ IITM TDS Demos
✔ Demo2 Checksum Quiz
✔ Audio Number Sum Tasks
✔ Vercel Multi-Level Quizzes
✔ Custom TDS Basic Quiz (10 Levels)

📌 Table of Contents

Overview

Features

Architecture

Project Structure

Installation

Configuration

Usage

API Endpoints

Tools

How It Works

Author

🔍 Overview

This project implements an autonomous quiz-solving agent for the Tools in Data Science (TDS) course.

The system:

✔ Scrapes dynamic web pages
✔ Extracts structured/hidden data
✔ Computes mathematical results
✔ Transcribes audio and extracts numbers
✔ Computes checksums
✔ Sends answers back to the server
✔ Automatically continues to the next question
✔ Fully autonomous (no human interaction required)

It uses Google’s Gemini 2.5 Flash LLM along with specialized tools to solve completely arbitrary quiz tasks.

✨ Features
🔹 Web Scraping

Uses Playwright to render dynamic HTML, JavaScript pages, and API-generated data.

🔹 Code Execution

LLM generates Python code → agent executes it safely → returns results.

🔹 Audio Transcription (Gemini)

Extracts numbers from audio, even with background noise.

🔹 File Downloading

Downloads PDFs, CSVs, and other files for processing.

🔹 Automatic Dependency Installation

LLM can request packages → agent installs them on-the-fly.

🔹 Intelligent Retry System

Prevents failure due to latency, wrong answers, or partial page loads.

🔹 Multi-Step Chain Solver

Solves sequential quizzes until the final level is reached.

🏗 Architecture
┌────────────────────────┐
│       FastAPI          │  ← Receives POST /solve
└──────────┬─────────────┘
           │
           ▼
┌────────────────────────┐
│    LangGraph Agent     │  ← Gemini 2.5 Flash controls logic
└──────────┬─────────────┘
           │
     ┌─────┼──────────────────────────────────────────────┐
     ▼     ▼              ▼               ▼                ▼
 [Scraper] [Downloader] [Code Runner] [Audio Tool]   [POST Submitter]

📂 Project Structure
LLM-Analysis-TDS-Project/
├── agent.py                     # LangGraph agent logic
├── main.py                      # FastAPI server
├── pyproject.toml               # Dependency definitions (uv)
├── tools/
│   ├── web_scraper.py           # Playwright rendered HTML scraper
│   ├── download_file.py         # Handles file downloads
│   ├── run_code.py              # Code execution sandbox
│   ├── add_dependencies.py      # Installs Python deps
│   ├── send_request.py          # POST submission tool
│   └── audio_transcribing.py    # Gemini audio → text → numbers
└── .env                         # Your secrets (not committed)

📦 Installation
✔ Prerequisites

Python 3.12+

uv package manager (recommended)

Playwright

Git

Step 1 — Clone the Repository
git clone your_repo_here
cd your_repo_here

Step 2 — Install Dependencies (Recommended: uv)
pip install uv
uv sync
uv run playwright install chromium


Start the server:

uv run main.py


Server runs on:

http://0.0.0.0:7860

✔ Option B — pip (not recommended)
python -m venv venv
venv\Scripts\activate
pip install -e .
playwright install chromium

⚙️ Configuration

Create a .env file in the project root:

EMAIL=23f2005290@ds.study.iitm.ac.in
SECRET=your_secret_here
GOOGLE_API_KEY=your_gemini_api_key

🚀 Usage
Start server
uv run main.py

Test using CURL
curl -X POST http://localhost:7860/solve \
  -H "Content-Type: application/json" \
  -d '{
    "email": "23f2005290@ds.study.iitm.ac.in",
    "secret": "your_secret_here",
    "url": "https://tds-llm-analysis.s-anand.net/demo"
  }'


You should get:

{"status": "ok"}


Your agent will now autonomously solve the quiz.

🌐 API Endpoints
POST /solve

Start solving a quiz.

GET /healthz

Health check.

🛠 Tools
✔ get_rendered_html()

Playwright-based scraper.

✔ run_code()

Executes Python generated by the LLM.

✔ transcribe_audio()

Gemini audio transcription → number extraction → sum.

✔ post_request()

Submits answers automatically.

✔ add_dependencies()

Installs missing Python packages.

🧠 How It Works

FastAPI receives a quiz URL.

Agent scrapes the page.

Gemini analyzes instructions.

Agent chooses the correct tool.

LLM generates code → agent executes it.

Answer is submitted back to the quiz server.

If new quiz URL is returned → next cycle begins.

Ends when no next URL is available.

🏆 Proven Successful On
✔ TDS Demo
✔ TDS Demo2 (checksum)
✔ TDS Audio Quiz
✔ Basic Test – 10 Levels
✔ TDS Vercel Test
✔ External quizzes with JSON API
✔ Dynamic JS pages & rendered tables

All solved during your testing logs.

👤 Author

Vipul Bhardwaj
Tools in Data Science (TDS)
IIT Madras